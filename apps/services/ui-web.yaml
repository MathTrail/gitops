apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mathtrail-ui-web
  namespace: argocd
spec:
  project: mathtrail
  source:
    repoURL: https://MathTrail.github.io/mathtrail-charts/charts
    chart: mathtrail-ui-web
    targetRevision: 0.1.0
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: mathtrail
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
